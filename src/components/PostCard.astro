---
interface Props {
  title: string;
  date: string;
  tags: string[];
  excerpt: string;
  href: string;
  lang: string;
  series?: string;
  seriesOrder?: number;
}

const { title, date, tags, excerpt, href, lang, series, seriesOrder } =
  Astro.props;
---

<a href={href} class="post-card card" data-lang={lang}>
  <h3>{title}</h3>
  <div class="meta">
    <span>📅 {date}</span>
    {
      series && (
        <span class="series-badge">
          📚 {series} #{seriesOrder}
        </span>
      )
    }
  </div>
  <p class="excerpt">{excerpt}</p>
  <div class="tags">
    {tags.map((tag) => <span class="tag">{tag}</span>)}
  </div>
</a>
